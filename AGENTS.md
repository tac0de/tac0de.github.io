# The Power of CSS

- Browser output is HTML and CSS only; no runtime JavaScript or canvas.
- Use dtp.config.json before broad edits.
- Visual direction: Kinetic Editorial, documented in docs/kinetic-editorial.md.
- Preserve native form state and src/state.css when changing presentation.
- Run node scripts/verify.mjs before handoff; it builds into a temporary directory.
- Do not edit blocked paths without explicit approval.
